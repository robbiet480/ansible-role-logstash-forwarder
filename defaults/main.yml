---
logstash_forwarder:
  timeout: 15
  servers:
    - host: localhost
      port: 5000
  ssl:
    source_dir: .
    destination_dir: /etc/pki/logstash
    ca_crt: logstash-forwarder-example.ca.crt
  files:
    - paths:
        - /var/log/messages
        - /var/log/auth.log
      fields:
        type: syslog
  # To add more sets of files...
  # - paths:
  #     - /var/log/boot.log
  #   fields:
  #     type: syslog